# Electrochemical workflow project

This is a minimal project template used by the team installer. Keep analysis
scripts and generated results here; keep raw EC-Lab files on the shared
measurement location.
The scripts use the sample `Type` from the live sheet when selecting folders:
`AEM` samples are searched under `AEM-WE`, other known types directly under the
year directory, and blank types in both locations.
